Drop Tosca .tsu exports here, or upload them on the hub page.

  npm run hub
  http://127.0.0.1:8765

The hub uploads into this folder, converts, and shows Jira + Allure reports on the same page.
The converter also reads this folder:

  npm run convert
